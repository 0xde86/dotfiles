# Personal Codex instructions

- Never execute commands with `sudo`, request approval to execute them, or wait for sudo authentication.
- When a task requires a privileged command, print the exact `sudo` command for the user to execute manually.
- Continue all non-privileged work before handing off any required privileged commands.
